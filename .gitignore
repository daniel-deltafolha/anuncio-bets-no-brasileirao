# Ignore data files but keep directory structure
dados/brutos/video_jogos/
dados/brutos/chave-google.json
dados/interim/frames/*
dados/interim/frames_bw/*
dados/interim/info_frames/*
dados/interim/info_frames_anotado/*
dados/interim/jogos_trim/*
dados/processados/*

# But keep .gitkeep files
!dados/interim/frames/.gitkeep
!dados/interim/frames_bw/.gitkeep
!dados/interim/info_frames/.gitkeep
!dados/interim/info_frames_anotado/.gitkeep
!dados/interim/jogos_trim/.gitkeep
!dados/processados/.gitkeep

# Ignore Google API key specifically (security)
dados/brutos/chave-google.json

# Keep the URLs file template but ignore it if it contains actual URLs
# Uncomment the line below if you want to ignore the URLs file:
# dados/brutos/urls_jogos.txt

# R specific ignores
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# OS specific
.DS_Store
Thumbs.db
